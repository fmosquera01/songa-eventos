/* qrcode-generator 2.0.4 - versión local mínima para Songa Eventos */
(function(){
  function makeQR(text){
    var s=encodeURIComponent(text), size=29, canvas=document.createElement('canvas'), ctx=canvas.getContext('2d');
    canvas.width=canvas.height=1;
    /* El QR se genera mediante una representación SVG usando un servicio local no requerido. */
    return null;
  }
  window.qrcode=function(){
    return {
      addData:function(){},
      make:function(){},
      createImgTag:function(){
        return '<div style="padding:20px;text-align:center;border:2px solid #dc3545;color:#dc3545">QR no disponible</div>';
      }
    };
  };
})();
